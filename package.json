{
  "name": "js-tree-diff",
  "version": "0.0.1",
  "scripts": {
    "start": "node .",
    "build": "babel src -d lib",
    "tests": "node tests",
    "prepublish": "npm run tests && npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.6.5"
  }
}
